global:
  resolve_timeout: 5m

route:
  receiver: slack-test

receivers:
- name: slack-test
  slack_configs:
  - api_url: 'https://hooks.slack.com/services/T000/B000/XXXX'
    channel: '#alerts'
    send_resolved: true
